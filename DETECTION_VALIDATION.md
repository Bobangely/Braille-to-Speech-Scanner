# การปรับ YOLO + OpenCV สำหรับจุดเบรลล์ขนาดเล็ก

บันทึกผลรอบก่อนบนภาพสังเคราะห์เดิม (legacy) ตารางไทยและโครงสร้างไฟล์ปัจจุบันอธิบายใน `docs/THAI_AND_CAMERA.md` ภาพเก่าเหล่านี้ไม่ใช่ชุดยืนยันรหัสไทยมาตรฐาน

ปรับบน branch `feature/yolo-detection` โดยใช้ weights เดิมใน `models/braille_yolo.pt` ไม่ได้เทรนหรือเปลี่ยนโมเดลในรอบนี้

## สิ่งที่เปลี่ยน

- Hybrid ใช้จุดจาก OpenCV ทุกครั้ง แม้ YOLO พบจุดเพียงบางส่วน แล้วรวมจุดซ้ำตามขนาดจุด โดยเก็บตำแหน่งศูนย์กลางจาก OpenCV
- ตรวจสีที่ความละเอียดต้นฉบับ ลดการลบจุดเล็กจาก blur และ morphology ใช้ศูนย์กลางแบบทศนิยม และลดพื้นที่ขั้นต่ำเฉพาะภาพที่จุดส่วนใหญ่มีขนาดเล็ก
- จุดที่ YOLO เสนอในโหมด Hybrid ต้องมีสีที่เลือกและรูปร่างกลมรองรับ ช่วยกรองเงา/ข้อความต่างสี โหมด YOLO ล้วนยังไม่ตรวจยืนยันสี
- ภาพที่ใหญ่กว่า 1280 พิกเซลตรวจเพิ่มเป็นส่วนย่อยซ้อนทับ 20% เพื่อรักษารายละเอียด รวมพิกัดกลับสู่ภาพต้นฉบับ และตัดจุดซ้ำ
- เพิ่มเพดาน YOLO เป็น 3000 จุดต่อการตรวจหนึ่งภาพ/ส่วนย่อย จากค่าปริยายของไลบรารี 300 จุด รองรับข้อความยาวขึ้น
- ประมวลผลส่วนย่อยทีละภาพ จำกัดไม่เกิน 16 ส่วนเพิ่มเติม หากเกินงบ ใช้ภาพรวมร่วมกับ CV และรายงาน `tile_budget_exceeded` แทนการตรวจเพียงบางมุมของภาพ
- เมื่อ YOLO เกิดข้อผิดพลาดใน Hybrid จะใช้ CV และส่งรายละเอียดใน `yolo_error` ส่วน worker ของกล้องจะล้างผลเก่าหากประมวลผลล้มเหลว
- ความนิ่งของผลกล้องเก็บประวัติจากผล AI ใหม่แต่ละครั้ง แก้โค้ดเดิมที่เก็บเฉพาะเมื่อข้อความเปลี่ยน และไม่นับผล AI เดิมซ้ำตามรอบวาดหน้าจอ

## วิธีใช้งาน

ตรวจภาพโดยระบุสีจุดให้ตรงกับภาพ (blue/red/green/black):

```powershell
.venv\Scripts\python.exe yolo_detector.py sample_images/test_hello_blue.png --mode hybrid --color blue --lang english --no-show
```

เปิดกล้องพร้อมตรวจภาพเป็นส่วนย่อย:

```powershell
.venv\Scripts\python.exe camera_reader.py --detector hybrid --res fhd --color blue --imgsz 1280 --tile-size 1280
```

หากบอร์ดประมวลผลช้า ให้เริ่มที่ `--tile-size 0` เพื่อปิดการตรวจส่วนย่อยเพิ่มเติม ส่วน CV ยังคงตรวจภาพต้นฉบับ การลด `--imgsz` ช่วยลดภาระ YOLO แต่ต้องวัดความแม่นยำซ้ำ โดยเฉพาะจุดเล็ก การซูมดิจิทัลไม่สามารถกู้รายละเอียดที่กล้องไม่ได้บันทึกไว้ได้

ไฟล์ `main.py` สำหรับอ่านภาพเดี่ยวยังคงใช้ OpenCV; ใช้ `yolo_detector.py` เมื่อต้องการทดสอบ Hybrid/YOLO

## ผลวัดจากภาพตัวอย่างในโปรเจกต์

ใช้ 39 ภาพที่มีข้อความอ้างอิงใน `tests/test_accuracy.py` ย่อภาพด้วย `INTER_AREA` วัดการอ่านตรงทั้งข้อความ ไม่ใช่คะแนน mAP ของโมเดล

| ระบบ | ขนาดเดิม | ย่อเหลือ 25% | ย่อเหลือ 15% |
|---|---:|---:|---:|
| OpenCV ก่อนปรับ | 39/39 | 39/39 | 0/39 |
| OpenCV หลังปรับ | 39/39 | 39/39 | 39/39 |
| Hybrid หลังปรับ ใช้ weights จริง | 39/39 | 39/39 | 39/39 |

OpenCV หลังปรับผ่านที่ 50% และ 20% เพิ่มเติมด้วย รายงาน Hybrid รายภาพอยู่ใน `output/legacy_benchmark.json` ซึ่งสร้างใหม่ได้ด้วยคำสั่งด้านล่าง

```powershell
.venv\Scripts\python.exe -m unittest discover -s tests -v
.venv\Scripts\python.exe benchmark_detection.py --dataset legacy --mode hybrid --scales 1 .25 .15 --output output/legacy_benchmark.json
```

ชุด regression ตรวจการอ่านหลายขนาด การรวมจุดเล็กที่อยู่ใกล้กัน การแปลงพิกัดส่วนย่อย การครอบคลุมภาพ การเติมจุดที่ YOLO พลาด การกรองจุดต่างสี การ fallback และลำดับผลของ worker โดยการทดสอบตรรกะบางส่วนใช้ผล YOLO จำลอง แยกจาก benchmark ที่โหลดโมเดลจริง

## ขอบเขตของผล

ผลนี้เป็น regression บนภาพตัวอย่าง ซึ่งอาจเกี่ยวข้องกับข้อมูลฝึกโมเดล จึงไม่ใช่หลักฐานความแม่นยำบนกระดาษใหม่หรือการใช้งานจริง จุดเล็กวัดเป็นพิกเซลในภาพ ไม่สามารถสรุปขนาดเป็นมิลลิเมตรได้หากยังไม่ทราบระยะกล้อง เลนส์ และโฟกัส

ยังต้องเก็บภาพจากกล้องบน Redxa Dragon Q6A ที่หลายระยะ แสง เงา มุมเอียง และสีแต้ม โดยแยกกระดาษ/ข้อความสำหรับทดสอบออกจากชุดฝึก แล้ววัดข้อความถูกทั้งประโยค จุดขาด/เกิน และเวลาประมวลผลบนบอร์ด การตรวจส่วนย่อยเพิ่มงาน YOLO จึงยังไม่รับประกันอัตราเฟรมบน Redxa และยังไม่ได้ทดสอบระบบเสียงกับอุปกรณ์จริงในรอบนี้
